const getCases = (req, reply) => {
    reply.send([]);
}

const getCase = () => {
    reply.send('case');
}

const addCase = () => {
    reply.send('case');
}

const updateCase = () => {
    reply.send('case');
}

const deleteCase = () => {
    reply.send('case');
}

export {
    getCases, 
    getCase,
    addCase, 
    updateCase, 
    deleteCase
}